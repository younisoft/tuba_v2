# TBOS My Properties — State Matrix

**Status:** Documentation only. Companion to `TBOS_MY_PROPERTIES_UX_ARCHITECTURE.md`.
**Sourcing discipline:** every row is marked `VERIFIED` (cites a Phase A audit section — either `OBSERVED BEHAVIOR` live or `CODE FACT`) or `FUTURE STATE` (a state this architecture requires but that Phase A could not observe, because observing it would have required an irreversible/destructive action, or because no such capability exists yet in legacy). No state below was invented without one of these two labels.

| State | Screen(s) | Sourcing | Description | Primary UI treatment |
|---|---|---|---|---|
| Loading (list) | PROP-01 | FUTURE STATE (`INFERENCE` — legacy's loading behavior wasn't specifically isolated in Phase A, but a list-loading state is a baseline requirement) | Property list fetching | Skeleton rows/cards |
| Empty — no properties at all | PROP-01 | FUTURE STATE | New account, zero properties ever created | Guided "add your first property" CTA, not a bare empty table |
| Empty — no results for filter/search | PROP-01 | FUTURE STATE | Filters applied, zero matches | "No properties match your filters" + clear-filters action |
| Active | PROP-01, PROP-02 | VERIFIED — `OBSERVED BEHAVIOR`, §4 of the audit (live "Active" tab, 2 entries) | Published, live listing | Status badge, green/success tone |
| Draft | PROP-01, PROP-02 | VERIFIED — `OBSERVED BEHAVIOR`, §5.1 of the audit (the residual draft created during live testing) | Created but not yet published — "Not Posted" | Status badge, neutral tone; "Complete Information" resume action |
| Pending | PROP-01 | VERIFIED — tab exists live (§4, count observed as 0 at audit time) but its triggering condition and detail view were not observed (§22 `OPEN QUESTION`) | Submitted, awaiting some review step — exact semantics unconfirmed | Status badge; detail content marked `OPEN QUESTION` pending Phase A follow-up |
| Sold | PROP-01 | VERIFIED — tab exists live, count 1 (§4) | Property marked sold | Status badge, terminal-state tone |
| Expired | PROP-01 | VERIFIED — tab exists live, count 20 (§4) | License/listing period lapsed | Status badge; should surface a clear "renew" path, matching the legacy Renew tag's intent (§12) |
| Rejected | PROP-01 | VERIFIED — tab exists live, count 5 (§4); the *reason* for rejection was never observed (§22 `OPEN QUESTION`) | Authority or internal rejection | Status badge; rejection-reason display is a `FUTURE STATE` requirement — no evidence legacy shows a reason at all |
| Canceled | PROP-01 | VERIFIED — tab exists live, count 25 (§4) | Broker-canceled listing | Status badge, terminal-state tone |
| Advertiser Type — defaulted from account | PROP-03 Flow A/B, Step 1 | FUTURE STATE (Decision 01 — a deliberate correction of legacy, which live-tested as always defaulting to Establishment regardless of the logged-in user, §5.2 of the audit) | Advertiser Type pre-selected from `agent_type` | Pre-filled selection, visibly editable, not disabled |
| Advertiser Type — changed by user | PROP-03 Flow A/B, Step 1 | FUTURE STATE (Decision 01) | User overrides the account default | Dependent fields (identity label/requirement, POA visibility, validation, Review Summary) recompute immediately, per architecture doc §4.3 |
| Advertiser Type — prefix hint (non-authoritative) | PROP-03 Flow A/B, Step 1/2 | FUTURE STATE (`INFERENCE` — a UX convenience, explicitly not a validation source per Decision 01) | Entered identifier's prefix suggests a different type than selected | Soft inline hint only; never auto-changes the selection, never substitutes for the Authority's own verification |
| Invalid format | PROP-03 Flow A/B | FUTURE STATE (`INFERENCE` — standard client-side validation; not exercised live, §22) | Malformed license/ID number, caught before any authority call | Inline field error, no network call made |
| Invalid license number | PROP-03 Flow A | FUTURE STATE — not observed live (§22); required per Decision 06 | Authority could not validate the license number itself | Mapped plain-language message (architecture doc §5), offers the Flow B escape hatch |
| Invalid advertiser identifier | PROP-03 Flow A/B | FUTURE STATE — not observed live (§22); required per Decision 06 | Authority could not validate the National ID/Unified Number | Mapped plain-language message, inline field error |
| License not found | PROP-03 Flow A | FUTURE STATE — not observed live (§22); required per Decision 06 | No license exists with the given details | Mapped plain-language message, offers the Flow B escape hatch |
| License belongs to another advertiser | PROP-03 Flow A | FUTURE STATE — not observed live (§22); required per Decision 06 | advertiserId/idType doesn't match the license's registration | Mapped plain-language message, no silent proceed |
| Advertiser/license type mismatch | PROP-03 Flow A | FUTURE STATE — not observed live (§22); required per Decision 06 | Advertiser Type and the license's registered type disagree | Mapped plain-language message, links back to Advertiser Type field |
| Authority rejection — business rule | PROP-03 Flow A/B | FUTURE STATE — none of the 29 documented error codes were observed live (§9, §22); the *requirement* to handle them is VERIFIED from the authority PDF's own error table | A specific NHC business-rule failure (e.g., price below contract price, no active license package) | Mapped plain-language message per the Authority Experience table (architecture doc §5), linked to the offending field/step |
| Authority unavailable | PROP-03 Flow A/B | FUTURE STATE — not observed live (§22); required per Decision 06 | Upstream proxy or NHC gateway outage (§6 of the audit) | Retry offered; Save Draft not blocked if independent of this call |
| Authority timeout | PROP-03 Flow A/B | FUTURE STATE — not observed live (§22); required per Decision 06 | Slow upstream response | Visible retry after a reasonable wait, never an indefinite spinner |
| Successful verification | PROP-03 Flow A | VERIFIED — `OBSERVED BEHAVIOR`, §5.2 of the audit (live validation against the supplied test license) | License found and valid | Proceeds to Review Retrieved Information |
| Missing information (pre-publish gate) | PROP-03 Review step | VERIFIED — this exact pattern (a specific, non-dead-end missing-requirement list) already exists in the currently-built PROP-03 wizard, predating this audit, and is preserved here | Required field/media missing at Review | Named list of what's missing, each linking back to its step (existing pattern, kept) |
| Success — Published | PROP-03 | VERIFIED — `OBSERVED BEHAVIOR`, §5.2 of the audit (live Package-info screen with "Publish Now") | Listing goes live | Explicit "Published — now Active" confirmation, matching existing two-terminal-state discipline |
| Success — Saved as Draft | PROP-03 | VERIFIED — same evidence as Draft row above | Listing saved, not published | Explicit "Saved as Draft" confirmation |
| Non-recoverable error (5xx) | PROP-03 (direct-nav case), any screen | VERIFIED — `OBSERVED BEHAVIOR`, §5.1 of the audit (reproduced 500 on direct `/properties/create` navigation) | Server error | Real error page with retry/support path, never a bare crash |
| Recoverable error — single failed media file | PROP-03 Property Setup step | VERIFIED — `CODE FACT`, the existing `MediaUploader` pattern component already implements this exact scoped-retry behavior (deterministic "fail" simulation), predating this audit | One file fails upload/processing | Retry affordance scoped to that file only; other files/fields unaffected |
| Promotion — active tier (Featured/Pro) | PROP-01, PROP-02 Promotion tab | VERIFIED — `OBSERVED BEHAVIOR`, §12 of the audit (live tier badges, "basic" observed on both test properties) | Current promotion tier | Tier badge |
| Promotion — available to upgrade | PROP-01, PROP-02, Promotion drawer | VERIFIED — `OBSERVED BEHAVIOR`, §12 (live license-validity dialog + functional upgrade modal with real balances) | License valid, upgrade offered | "Promote" action enabled |
| Promotion — unavailable (license expiring/expired) | Promotion drawer | FUTURE STATE (`INFERENCE` from the live `checkAdLicenseDate` guard's existence, §12 — the guard clearly implies a below-threshold blocking case, but a fully-expired/blocked outcome was not driven to completion live) | License too close to/past expiry to promote | Explicit explanation + link to renew, not a disabled button with no reason |
| Promotion — insufficient balance | Promotion drawer | FUTURE STATE (`INFERENCE` — the live modal only ever showed "enough credit" states; a zero-balance state was not observed, §22) | No package credit and user hasn't chosen Credit Card | Balance shown as unavailable, Credit Card path defaults/highlighted |
| Service — available | PROP-01 overflow, PROP-02 Services tab, Service Request drawer | VERIFIED — `OBSERVED BEHAVIOR`, §13 (live Photography modal, real "1 Photograph Service Left" balance) | Service bookable | Request action enabled |
| Service — requested | Service Request drawer, PROP-02 Services tab | FUTURE STATE — booking was never submitted live (§13, to avoid an irreversible payment/credit action); more importantly, the code-level fulfillment handler is a confirmed empty stub (§9/§13), so this state currently has **no real backend behind it** | A request has been submitted, awaiting fulfillment | Must not overstate certainty — copy should say "Requested" without implying a tracked fulfillment pipeline exists yet |
| Service — unavailable (Drone via Package) | Service Request drawer, Drone specifically | VERIFIED — `CODE FACT`, §13 (Drone's `type` value confirmed excluded from the package-deduction endpoint's allow-list) | Drone requested via Package payment | Package option must not be offered for Drone until the backend gap is fixed; Credit Card path only, or a clear "not available via package" explanation |
| Performance — Reach available | PROP-01 (quick summary), PROP-02 Performance tab | VERIFIED — `OBSERVED BEHAVIOR`, §14 (live numbers, 778/12,757 observed) | Real counter, `CURRENTLY VERIFIED` | Numeric display |
| Performance — Views available | PROP-01 (quick summary), PROP-02 Performance tab | VERIFIED — `OBSERVED BEHAVIOR`, §14 (live numbers, 170/439 observed) | Real counter, `CURRENTLY VERIFIED` | Numeric display |
| Performance — Leads (displayed, structurally always zero) | PROP-02 Performance tab only (not shown at list density, architecture doc §8.6) | VERIFIED — `CODE FACT`, §14 (the one `leads` increment call site is commented out); classified `CURRENTLY VERIFIED (displayed) / BACKEND-DEPENDENT (pipeline)` per architecture doc §8.1 | Leads metric that can never currently be non-zero | "Not yet reliably tracked" treatment rather than a bare `0` — `BUSINESS DECISION REQUIRED` on exact wording/visual treatment |
| Performance — Clicks | PROP-02 Performance tab | FUTURE STATE — `FUTURE/BACKEND-DEPENDENT`, no such field exists anywhere in legacy (§14 of the audit) | Net-new metric, no data source | Rendered in the KPI layout as "not available yet," never fabricated |
| Performance — Contacts / Calls / WhatsApp / Visit Requests / Favorites / Shares | PROP-02 Performance tab | FUTURE STATE — `FUTURE/BACKEND-DEPENDENT`, none observed anywhere in Phase A | Net-new metrics, no data source | Same "not available yet" treatment as Clicks — visible in the architecture, not real data |
| Performance — derived metrics (CTR, conversion rates, engagement rate) | PROP-02 Performance tab | FUTURE STATE — explicitly not implemented per Decision 02/§8.3 of the architecture doc | Requires both underlying metrics to be `CURRENTLY VERIFIED` and a formally agreed calculation | Not rendered as a number until both conditions are met |
| Performance — trend view | PROP-02 Performance tab | FUTURE STATE — legacy's only chart is decorative/hardcoded (§14) | No real time-series data exists yet | Chart slot present in layout, renders nothing until backed by real data — never a fake/sample chart |
| Performance — unavailable (new property) | PROP-02 Performance tab | FUTURE STATE (`INFERENCE` — a genuinely-new property with zero traffic is a plausible near-term state even though not specifically observed) | New/unpublished property, no data yet | "No performance data yet" (distinct from the Leads-specific "not yet reliably tracked" message above, and distinct from a `FUTURE/BACKEND-DEPENDENT` metric's "not available yet") |
| Restricted (permission denied) | Any screen/tab | VERIFIED — `CODE FACT` pattern already established platform-wide (existing `RestrictedState` component, and the P0 cross-agency regression pattern already fixed in prior phases per project memory) | User lacks the required permission/scope | `RestrictedState` component, existing pattern, reused as-is |

## Explicitly out of scope for this matrix

States requiring an irreversible live action to observe (a completed Promotion purchase, a completed Service booking, an actual authority rejection, a fully-expired-license promotion attempt) were **not** driven to completion in Phase A per its own stop condition (§8 of the audit's governing instructions), and are marked `FUTURE STATE` above rather than fabricated. Closing these `OPEN QUESTION`s (also listed in the audit's §28) is recommended before Phase C implementation locks in exact copy/behavior for them.
