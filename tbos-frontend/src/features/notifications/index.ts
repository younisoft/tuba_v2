/** Extension point for the Notifications module (NOTIF-01/02) — the shell's
 * NotificationBell (components/navigation/NotificationBell.tsx) already proves
 * the underlying data path; this is where the full NOTIF-01/02 screens land. */
export {};
