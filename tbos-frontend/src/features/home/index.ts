/**
 * Extension point for the Home module (HOME-01). Currently rendered by the
 * generic ScreenPlaceholder via app/router — a future feature phase adds real
 * screen components here and swaps the override into app/router/index.tsx.
 */
export {};
